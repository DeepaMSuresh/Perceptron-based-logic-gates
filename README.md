# Perceptron-based-logic-gates
Repository containing Python training scripts, fault-aware datasets, Verilog HDL designs, and testbenches for perceptron-based fault-resilient digital circuits. The framework models logic gates, arithmetic units, and sequential circuits resilient to transient SEE/SET-induced input faults.
